{
  "name": "permitproof-dashboard-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Local three-port PermitProof dashboard prototype",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "build": "npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=18"
  }
}
